class splice_site:
	def __init__(self, scaffold_id, sequence, index, score, relative_score):
		self.scaffold_id = scaffold_id
		self.sequence = sequence 
		self.index = index 
		self.score = score 
		self.relative_score = relative_score 
		